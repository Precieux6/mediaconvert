version: '3.8'

services:
  backend:
    build: ./backend
    container_name: mediaconvert_backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend/uploads:/app/uploads
    restart: always
